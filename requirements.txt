pydantic>=2.0
langgraph>=1.2.0
langgraph-checkpoint-sqlite>=2.0.0
streamlit>=1.40.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Optional: only needed if you use app/llm/claude_planner.py (real Claude API).
# Without a key, both the Streamlit UI and the FastAPI /tasks endpoint run in
# demo mode using SingleToolDemoPlanner instead -- see app/llm/demo_planner.py.
# anthropic>=0.40.0
